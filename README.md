# YouTube Downloader

**YouTube Downloader** — це зручний графічний застосунок для завантаження відео та аудіо з YouTube. Проєкт написаний на Python з використанням `Tkinter` та `yt-dlp`.

---

## Особливості

- Завантаження відео у форматі **MP4**
- Конвертація аудіо у формати **MP3** або **WAV**
- Підтримка вибору папки для збереження файлів
- Прогрес-бар для відображення стану завантаження
- Автоматична перевірка та оновлення `yt-dlp`
- Підтримка роботи з PyInstaller для створення `.exe`

---

## 🛠 Встановлення

1. **Клонуйте репозиторій:**
```bash
   git clone https://github.com/PavloPatrylo/YouTube-Downloader.git
   cd YouTube-Downloader
```

2. **Встановіть Python** (3.9+ рекомендовано)

3. **Не потребує встановлення пакетів**

4. **Завантажте FFmpeg:**
   - Перейдіть на [FFmpeg Builds](https://www.gyan.dev/ffmpeg/builds/) та завантажте `ffmpeg.exe`
   - Покладіть його у ту ж папку, що й скрипт `youtube_downloader.py`
   - `yt-dlp` буде автоматично завантажений програмою при першому запуску

---

## Використання

1. Запустіть застосунок:
```bash
   python youtube_downloader.py
```

2. Введіть URL відео YouTube

3. Виберіть папку для збереження (за замовчуванням — `downloads`)

4. Виберіть формат файлу: `mp4`, `mp3`, або `wav`

5. Натисніть **Завантажити**

6. Дочекайтеся завершення завантаження (відображається прогрес)

---

## Підтримка PyInstaller

Щоб створити `.exe` для Windows:
```bash
pyinstaller --onefile --windowed youtube_downloader.py
```

> **Примітка:** переконайтеся, що `ffmpeg.exe` знаходиться поруч із згенерованим `.exe`

---

## Увага

- Файл `ffmpeg.exe` не додається до репозиторію через обмеження GitHub
- Для конвертації аудіо у MP3/WAV потрібен FFmpeg
- Використовуйте програму тільки для завантаження контенту, який дозволено завантажувати за умовами YouTube

---

## Автор

**Pavlo Patrylo**
